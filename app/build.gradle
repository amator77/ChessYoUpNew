apply plugin: 'com.android.application'

android {
    signingConfigs {
        chessyoup {
            keyAlias 'chessyoup'
            keyPassword 'leo@1977'
            storeFile file('/home/leo/private/chess/ChessYoUp/keystore2.jks')
            storePassword 'leo@1977'
        }
    }
    compileSdkVersion 20
    buildToolsVersion "20.0.0"
    defaultConfig {
        applicationId 'com.chessyoup'
        minSdkVersion 14
        targetSdkVersion 20
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.chessyoup
    }
    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.google.android.gms:play-services:6.1.11'
    compile project(':BaseGameUtils')
}
